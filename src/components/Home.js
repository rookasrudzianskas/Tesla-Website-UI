import React from 'react';
import styled from "styled-components";

const Home = () => {
    return (
        <Container>
            <Header>

            </Header>
        </Container>
    );
};

export default Home;


const Container = styled.div`
  height: 100vh;  
`